module top_module( 
    input [255:0] in,
    input [7:0] sel,
    output out );

    assign out = in[sel]; //Index 'in' with sel, wires see everything as binary, so indexing with a binary number works
    
endmodule
