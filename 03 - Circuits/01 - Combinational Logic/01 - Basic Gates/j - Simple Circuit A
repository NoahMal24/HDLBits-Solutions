module top_module (input x, input y, output z);

    module_A helper_gate(
        .x1(x),
        .y1(y),
        .z1(z)
    );
    
endmodule

module module_A (input x1, input y1, output z1); //Practice with writing modules as helper functions
    assign z1 = (x1^y1) & x1;
endmodule
