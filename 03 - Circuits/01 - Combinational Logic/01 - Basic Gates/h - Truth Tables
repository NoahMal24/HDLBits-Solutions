module top_module( 
    input x3,
    input x2,
    input x1,  // three inputs
    output f   // one output
);

    assign f = (~x3 & x2) | (x3 & x1); //Simplified it down to make it shorter and more efficient of a logic equation
    
endmodule
